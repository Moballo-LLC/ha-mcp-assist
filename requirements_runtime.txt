# Mirrors custom_components/mcp_assist/manifest.json requirements for Dependabot
# visibility only. Keep these no stricter than the manifest so this mirror does
# not narrow the Home Assistant compatibility window by accident.
aiohttp>=3.8.0
pyyaml>=6.0
ddgs>=6.0.0
websockets>=12.0
cryptography>=49.0.0
