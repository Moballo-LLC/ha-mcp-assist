# Mirrors custom_components/mcp_assist/manifest.json requirements so Dependabot
# can see the integration's runtime dependency surface.
aiohttp>=3.14.1
pyyaml>=6.0.3
ddgs>=6.0.0
websockets>=16.0
cryptography>=49.0.0
