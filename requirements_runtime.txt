# Mirrors custom_components/mcp_assist/manifest.json requirements for dependency
# visibility only. Dependabot version updates exclude this file; keep these no
# stricter than the manifest so the mirror cannot narrow Home Assistant support.
aiohttp>=3.8.0
pyyaml>=6.0
ddgs>=6.0.0
websockets>=12.0
cryptography>=41.0.0
